#!/usr/bin/python
# -*- coding: utf-8 -*-

import imaplib

obj = imaplib.IMAP4_SSL('imap.hermes.cam.ac.uk', 993)
obj.login('cf443', open('/home/craigfe/.password_email').read().rstrip('\n'))
obj.select()

print('  ' + str(len(obj.search(None, 'unseen')[1][0].split())))
